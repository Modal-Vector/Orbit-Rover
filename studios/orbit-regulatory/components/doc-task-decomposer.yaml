component: doc-task-decomposer
prompt: prompts/doc-task-decomposer.md

delivers:
  - .orbit/plans/regulatory/atomic/

success:
  when: bash
  condition: |
    test -d .orbit/plans/regulatory/atomic && \
    find .orbit/plans/regulatory/atomic -name '*.json' | head -1 | grep -q .

orbits:
  max: 5
