component: source-decomposer
prompt: prompts/source-decomposer.md

delivers:
  - .orbit/plans/sentinel/tasks.json

orbits:
  max: 3
  success:
    when: file
    condition: .orbit/plans/sentinel/tasks.json
