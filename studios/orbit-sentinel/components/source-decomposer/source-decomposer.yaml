component: source-decomposer
prompt: components/source-decomposer/source-decomposer.md

delivers:
  - "{mission.run_dir}/plans/sentinel/tasks.json"

orbits:
  max: 3
  success:
    when: file
    condition: "{mission.run_dir}/plans/sentinel/tasks.json"
