component: topic-decomposer
prompt: prompts/topic-decomposer.md

delivers:
  - .orbit/plans/research/atomic/

orbits:
  max: 5
  success:
    when: bash
    condition: |
      test -d .orbit/plans/research/atomic && \
      find .orbit/plans/research/atomic -name '*.json' | head -1 | grep -q .
